					today = new Date();
					BigDay = new Date("June 11, 2010")
					msPerDay = 24 * 60 * 60 * 1000 ;
					timeLeft = (BigDay.getTime() - today.getTime());
					e_daysLeft = timeLeft / msPerDay;
					daysLeft = Math.floor(e_daysLeft);
					e_hrsLeft = (e_daysLeft - daysLeft)*24;
					hrsLeft = Math.floor(e_hrsLeft);
					minsLeft = Math.floor((e_hrsLeft - hrsLeft)*60);
					
					//if (daysLeft  0) 
					if (BigDay != 0)
					{
				//	var str = "<h4>Permohonan Jawatan Telah Ditutup</h4>";
					//document.write(str.fontcolor("red"));
					window.location = "index-finish.html"

					}
					else
					{
					//Informasi
					//document.write("<h4>Permohonan Jawatan Telah Ditutup</h4>");
						//var str = "<h4>Permohonan Jawatan Tinggal " + hrsLeft +" jam dan " + minsLeft + " minit lagi sebelum ditutup</h4>";
					//	document.write(str.fontcolor("red"));
					
					}